<!DOCTYPE html>
<html>
<head>
  <title>Moneysave</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>

<body>

  <div class="container">

    <div class="masthead">
      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Gimme Oro</a>
          </div>
          <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav">

              <li class="<%= request.path == entries_path ? 'active' : '' %>"><%= link_to "Entries", entries_path %></li>
              <li class="<%= request.path == budgets_path ? 'active' : '' %>"><%= link_to "Budgets", budgets_path %></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
              <li class="<%= request.path == csv_import_path ? 'active' : '' %>"><%= link_to "CSV Import", csv_import_path %></li>
              <li><%= link_to "Sign Out", destroy_user_session_path, :method => :delete %></li>
            </ul>
          </div>
        </div><!--/.container-fluid -->
      </nav>


    </div>

    <% if @date %>
    <div>
      <h3><small><%= link_to "<<", entries_path(date: @date - 1.month) %></small>
          <span><%= @date.strftime "%B" %></span>
          <small><%= link_to ">>", entries_path(date: @date + 1.month) %></small></h3>
    </div>
    <% end -%>

    <%= yield %>

    <hr>

    <div class="footer">
      <p>© Bla bla bla 2014</p>
    </div>

  </div> <!-- /container -->

</body>
</html>
